# 멈칫(Meomchit) — 프로토타입 구현 명세서 (SPEC)

> AI파수꾼 팀 / AWS 퍼블릭 클라우드 기반 / 목표: **동작하는 목업(mockup)**
> 이 문서는 Claude Code(CLI)가 구현 시 따라야 할 단일 기준(single source of truth)이다.

---

## 0. 이 프로토타입이 증명하려는 것

비대면 송금 직전, AI Agent가 고객과 대화하며 **자발적 송금형 보이스피싱**을 진단하고 영업점 연계까지 지원한다. 평가자가 "이렇게 흘러가는구나, 이런 기술(Bedrock·RAG·OCR·Agent)이 들어갔구나"를 체감할 수 있는 수준의 **동작하는 목업**을 만든다. 완성도 높은 세부기능이 아니라 **플로우가 실제로 작동**하는 것이 목표.

**핵심 차별점 (구현에 반영할 정체성)**
- FDS 대체가 아니라 **보완**: FDS는 '본인 여부'만 검증 → 본인이 속아서 보내는 자발적 송금형은 못 잡음. 우리는 '송금 의도의 맥락'을 본다.
- **맥락이 결정타, 계좌신호는 보조**: 계좌점수가 낮아도 맥락이 위험하면 위험 판정(오탐 억제 + 신종사기 포착).
- 네 층위를 본다: 계좌 데이터(1단계) + 진술 맥락(선택지) + 행동 심리(입력 행위) + 내용 분석(RAG).

---

## 1. 기술 스택 및 프로바이더 전략

**개발 기본은 로컬 오픈소스, AWS는 전환 가능하게 설계한다.** 공모전은 AWS 필수가 아닌 권장이므로, 로컬에서 AWS 없이 100% 개발·시연하되, LLM·OCR·RAG 세 축을 **프로바이더 스위치**로 추상화해 설정만 바꾸면 AWS 버전으로 전환되게 한다. (두 환경 모두 동작 목표)

| 축 | 로컬(개발 기본) | AWS(전환용) | 스위치 |
|---|---|---|---|
| LLM | Anthropic API 직접 | AWS Bedrock Claude | `LLM_PROVIDER = anthropic \| bedrock` |
| OCR | Tesseract (오픈소스) | Amazon Textract | `OCR_PROVIDER = tesseract \| textract` |
| RAG / 임베딩 | FAISS + 오픈소스 임베딩(한국어 sentence-transformers) | Bedrock KB + Titan | `RAG_BACKEND = faiss \| bedrock_kb` |
| STT(음성파일) | 오픈소스(whisper 등) 또는 생략 | Amazon Transcribe | `STT_PROVIDER = local \| transcribe` |

**추상화 원칙**: 호출부는 공통 인터페이스로 감싸고, 구현체만 설정으로 교체한다.
```python
llm = get_llm()            # 설정 보고 anthropic/bedrock 구현 반환
result = llm.chat(...)     # 호출부는 프로바이더와 무관하게 동일
ocr = get_ocr()            # tesseract/textract
rag = get_rag()            # faiss/bedrock_kb
```

| 구분 | 기술 |
|---|---|
| 프론트엔드 | React (모바일 세로 레이아웃) |
| 백엔드 | FastAPI (Python) |
| LLM | 프로바이더 스위치 (Anthropic API / Bedrock) |
| OCR | 프로바이더 스위치 (Tesseract / Textract) |
| RAG | 프로바이더 스위치 (FAISS / Bedrock KB) |
| 실행 환경 | 로컬 노트북 (개발) / Amazon VDI (AWS 버전 검증) |

**설계 원칙**
- 위험등급은 **결정론적 룰(scoring.py)**로 산출 → 재현·설명 가능. LLM은 대화·해석만 담당(환각 배제).
- 1단계 계좌 8개 툴은 **항상 전량 호출**. 2단계 OCR·STT·RAG는 **고객이 자료를 올렸을 때만 조건부 호출**.
- 실데이터 없는 부분은 **합성 Mock**. 실고객정보·실제 문자 절대 사용 금지.

### 실행 모드 (3단 스위치) — 처음부터 이 구조로 구현할 것

프론트는 "AI 응답을 어디서 받는지"를 **환경설정 하나로 전환**할 수 있어야 한다. 화면 코드는 하나로 공유하고, 응답 소스만 스위치한다. (나중에 급조하면 화면을 뜯어고쳐야 하므로 반드시 초기부터 반영)

```
RESPONSE_SOURCE = "demo" | "local" | "remote"
```

| 모드 | 응답 소스 | 용도 | AWS 키 | 배포 |
|---|---|---|---|---|
| **demo** | 프론트에 내장된 시연 5케이스 고정응답 재생 | 발표·외부 공유(QR/링크) | 불필요 | 프론트만 Vercel 등 |
| **local** | 로컬/VDI FastAPI + 실제 LLM·RAG (프로바이더 설정에 따름) | 개발·검증·실구동 시연 | 프로바이더에 따라 | 안 함(localhost) |
| **remote** | 외부 배포된 백엔드 주소 | (선택) 백엔드까지 외부 공개가 필요할 때 | 필요 | 별도 인프라·회사 승인 필요 |

**원칙**
- 화면·상태전환 로직은 세 모드 공통. `demo`는 각 시연 케이스 선택 시 정해진 흐름을 각본대로 재생(백엔드 호출 없음).
- `demo` 응답 데이터는 5장 시연 케이스의 검산 결과(계좌점수·맥락점수·최종판정·요약·리포트)를 그대로 내장.
- `remote`는 문만 열어둔다. 실제 백엔드 외부배포는 프로토타입 필수 아님 — API 키 노출·회사 보안·비용 리스크가 있어, 필요성이 확실하고 승인된 경우에만 전환.
- 발표 안전장치: 현장 시연은 `demo`(네트워크·LLM 장애와 무관하게 항상 동작)를 기본으로, "진짜 동작"은 `local`로 별도 시연.

---

## 2. Mock 데이터 스키마

### 2-1. 고객 프로필 + 거래이력 (출금계좌 = 본인) — 4명

```json
{
  "customer_id": "C001",
  "name": "남용환",
  "account": "351-0000-0001",
  "balance": 8050864,
  "baseline": {
    "avg_transfer_amount": 300000,
    "max_transfer_amount": 1000000,
    "usual_hours": [9, 22],
    "transfer_limit": 5000000
  },
  "recent_transactions": [
    {"date": "2026-08-01", "amount": 250000, "time": "14:30", "payee": "P010"},
    {"date": "2026-08-05", "amount": 300000, "time": "19:00", "payee": "P011"}
  ],
  "recent_events": {
    "limit_changed": false,
    "limit_changed_at": null,
    "fund_source": null,
    "device_new": false
  }
}
```

**4명 설정 요약** (상세 값은 시연 시나리오(5장)에 맞춰 생성)
- **C001 남용환**: 평소 소액(평균 30만). 케이스1(검찰사칭)·케이스4(중고거래) 재사용.
- **C002**: 대출 갈아타기 상황용. 한도 상향 이력 세팅.
- **C003**: 메신저피싱 상황용. 소액 송금.
- **C004**: 평소 큰 거래도 하는 고객(avg/max 높게). 부동산 잔금 정상 케이스용(오탐 억제 시연).

### 2-2. 수취계좌 DB (외부 조회 대상)

```json
{
  "payee_account": "010-6660-98261",
  "payee_name": "남용환",
  "is_fraud_reported": true,
  "fraud_report_count": 3,
  "account_age_days": 5
}
```

여러 건을 리스트로 보유. 시연 케이스별 수취계좌를 여기 등록.

### 2-3. 사기 시나리오 지식베이스 (RAG용) — 사례집 10건

> 실제 사례집 파일(경찰청 월간피싱 zero / NH 제작)을 받아 수작업 정제 예정. **사례 1건 = 청크 1건.**

```json
{
  "id": "S03",
  "중분류": "카드배송원→검사사칭",
  "수법요약": "카드배송원 사칭 접근 후 금감원·검찰 사칭으로 이어져 원격앱 설치·안전계좌 유도",
  "범인멘트": ["OO카드 배송기사입니다", "금융감독원 자산 보호 조치", "OO원격제어 앱 설치", "안전계좌로 이체"],
  "위험신호": ["원격제어앱", "자산보호", "안전계좌", "수사기관사칭"],
  "유형": "기관사칭",
  "위험도": "고",
  "출처": "경찰청 월간피싱 zero"
}
```

임베딩 대상: `수법요약 + 범인멘트 + 위험신호`. `유형·위험도·출처`는 메타데이터.

---

## 3. 툴 입출력 계약 (Tool Contracts)

모든 1단계 툴은 공통 형식 반환: `{signal, hit, score, detail}`

### 3-1. 1단계 — 계좌 신호 툴 (항상 전량 호출)

```
check_payee_fraud(payee_account)
  → {signal:"payee_fraud", hit:bool, score:0|40, detail:"사기신고 3건"}

check_amount_anomaly(customer_id, amount)
  → {signal:"amount_anomaly", hit:bool, score:0|8|15|25, detail:"평소 대비 66배"}

check_fund_source(customer_id)
  → {signal:"fund_source", hit:bool, score:0|25, detail:"적금해지 직후"}

check_payee_freshness(payee_account)
  → {signal:"payee_freshness", hit:bool, score:0|10|20, detail:"개설 5일"}

check_limit_change(customer_id)
  → {signal:"limit_change", hit:bool, score:0|20, detail:"직전 한도상향"}

check_velocity(customer_id)
  → {signal:"velocity", hit:bool, score:0|10|20, detail:"10분내 3건"}

check_device(customer_id)
  → {signal:"device", hit:bool, score:0|15, detail:"신규 기기"}

check_time_pattern(current_time, customer_id)
  → {signal:"time_pattern", hit:bool, score:0|5|10, detail:"평소 없는 새벽"}
```

### 3-2. 2단계 — 맥락 툴 (조건부 호출)

```
ocr_extract(image_ref)   → {text:"추출된 문자내용..."}
stt_extract(audio_ref)   → {text:"녹음 전사내용..."}
scenario_rag(text)
  → {signal:"scenario", hit:bool, matched_type:"기관사칭", similarity:0.87,
     score:0|30|50, risk_signals:["안전계좌","원격앱"], source:"경찰청 사례집 S03"}
```

### 3-3. 3단계 — 취합 함수 (툴 아님)

```
calculate_final_risk(account_signals[], context_signals[])
  → {account_level:"중", context_level:"고", final:"위험",
     reasons:[...], action:"영업점유도"}
```

---

## 4. 스코어링 고정 로직 (데모용 확정값)

### 4-1. 1단계 계좌 점수

```
payee_fraud:      사기신고 이력 → 40
amount_anomaly:   평소 평균 대비 ≥10배(또는 이력없음+100만↑) → 25 / 5~10배 → 15 / 2~5배 → 8 / <2배 → 0
fund_source:      예·적금 해지 후 24h 내 → 25
payee_freshness:  개설 7일 이내 → 20 / 30일 이내 → 10
limit_change:     24h 내 한도 상향 → 20
velocity:         10분내 3건+ → 20 / 2건 → 10
device:           신규 기기/환경 → 15
time_pattern:     00~06시 → 10 / 평소 시간대 밖 → 5
```
**1단계 등급**: `0~29 저 / 30~59 중 / 60+ 고`
※ amount_anomaly 기준선은 `baseline.avg_transfer_amount`. 평소 큰 금액 보내던 고객은 배수가 낮아 정상 처리(오탐 억제).

### 4-2. 2단계 맥락 점수

```
[선택지 가중]
  위험선택지(최근연락받음 / 대출·투자 등) → +25
  하드오버라이드 선택지(기관안내 / 안전계좌 / 앱설치) → +50
  정상선택지 → 0
[행동 가중]
  대화창 자유입력 또는 첨부 실행 → +10   (건너뛰기는 감점 없음)
[RAG 분석]
  고위험 유형 매칭 + similarity ≥0.80 → +50
  similarity 0.60~0.79 → +30
  similarity <0.60 → 0 (단 '안전' 아님 — LLM 보조판단)
  결정적 위험신호 직접 hit(안전계좌·원격앱 등) → 하드오버라이드
```
**2단계 등급**: `0~24 저 / 25~49 중 / 50+ 고` (하드오버라이드 시 즉시 고)

### 4-3. 3단계 최종 매트릭스

```
             계좌 高   계좌 中   계좌 低
  맥락 高       🔴        🔴        🔴
  맥락 中       🔴        🟡        🟡
  맥락 低       🟡        🟢        🟢

하드오버라이드(위험계좌 확정 / URL 피싱 / 결정 키워드) → 무조건 🔴
```

### 4-4. 개입 강도 (1단계 등급 → 대화)

| 등급 | 개입 |
|---|---|
| 🟢 저 | 확인 1탭 (질문 없음) |
| 🟡 중 | 공감형 질문 1개 → AI 대화창 |
| 🔴 고 | 공감형 질문 1개 + 안전질문 1개 → AI 대화창 |

**질문 톤 원칙**: 이름 부르기 / 취조 아닌 배려("어떻게 도와드릴까요") / 위험표시(⚠️)는 고객 화면에 노출 X / 정상 선택지를 앞에 배치.

**공통 안전질문(고위험만)**: "혹시 검찰·금감원이라며 '안전계좌'로 옮기라거나, 앱 설치를 안내받으셨나요?" [아니요 / 비슷한 안내 받음(+50)]

**AI 대화창 문구**: "현재 송금이 안전한지 AI가 분석해드릴 수도 있어요. 상황 설명, 대화 캡처, 통화 녹음 뭐든 올려주시면 확인해드릴게요." [입력] [첨부] [건너뛰기]

---

## 5. 시연 시나리오 5케이스 (검산 완료)

### 케이스 1 — 검찰사칭 🔴 (대표 데모)
C001, 적금해지 후 2,000만원 → 신고이력 계좌
- 1단계: payee_fraud(40)+amount_anomaly(25)+fund_source(25) = **90 계좌 高**
- 2단계: 기관안내(+50)+첨부(+10)+RAG 0.87(+50) = **110 맥락 高**
- 결과: **🔴** (하드오버라이드) → 영업점 유도 + 요약리포트

### 케이스 2 — 대환대출 🔴
C002, 한도 상향 직후 500만원 → 신규계좌
- 1단계: amount_anomaly(25)+limit_change(20)+payee_freshness(20) = **65 계좌 高**
- 2단계: 대출·투자(+25)+입력(+10)+RAG 0.82(+50) = **85 맥락 高**
- 결과: **🔴**

### 케이스 3 — 메신저피싱 🔴 (계좌 낮음 → 맥락으로 포착, 핵심 데모)
C001, 카톡 받고 100만원 → 신규계좌
- 1단계: amount_anomaly(8)+payee_freshness(20) = **28 계좌 저**
- 2단계: "아는사람"→후속"문자로만 연락"(+30)+입력(+10)+RAG 0.6(+30) = **70 맥락 高**
- 결과: **🔴** — FDS가 못 잡는 저(低)계좌점수 사기를 맥락으로 포착

### 케이스 4 — 중고거래 정상 🟢 (편의성 데모)
C001, 15만원 → 신규계좌
- 1단계: payee_freshness(20) = **20 계좌 저** → 질문 없이 확인 1탭
- 결과: **🟢** (3초 통과)

### 케이스 5 — 부동산 잔금 정상 🟢 (오탐 억제 데모)
C004(평소 큰 거래도 함), 5,000만원 → 신규계좌
- 1단계: amount_anomaly + payee_freshness(20) = **45 계좌 중**
- 2단계: "거래대금"(0)+건너뛰기 = **0 맥락 저**
- 결과: **🟢** — 금액 커도 정상사유면 안 막음(통장묶기 민원 방지)

---

## 6. 모바일 UI — 화면 구성 및 전환

> **디자인 방침**: 지금은 기능 목업 단계. 디자인 디테일은 후속. 시각 톤만 **NH 그린 계열(#2E9A4A 근처) + 모바일 세로 레이아웃**으로 통일. 폰트·여백·아이콘은 구현 재량.

### 6-0. 핵심 UX — 1단계는 백그라운드 선(先)실행

1단계 계좌 스코어링은 **송금 버튼을 누를 때가 아니라, 금융기관·계좌 선택이 끝난 시점에 백그라운드로 미리 실행**한다. 그래야 송금 버튼을 눌렀을 때 로딩 없이 즉시 다음 화면(확인/질문)으로 넘어간다.

### 6-1. 화면별 정의

```
[M1] 송금액 입력
     - 출금계좌(고객 본인) 표시, 금액 입력 키패드
     - [다음]

[M2] 수취 금융기관·계좌 선택
     - 은행 선택 + 계좌번호 입력 → 예금주 조회 표시
     - ⟳ 이 화면 진입/완료 시점에 1단계 8개 툴 백그라운드 실행 → 등급(저/중/고) 확보
     - [송금]

[M3] 분기 (M2에서 확보한 등급으로 즉시 결정)
     - 저위험 → [M3a] 확인 팝업
        "최근 보낸 적 없는 분이에요. OOO님께 이체할까요?" [취소][확인]
        → 확인 시 [M7] 송금 완료
     - 중/고위험 → [M4] 질문 카드

[M4] 공감형 질문 (중=1개 / 고=1개 + 안전질문 1개)
     - 카드형, 선택지 버튼(정상 선택지 앞 배치, ⚠️ 미표시)
     - 답변 선택 시 → [M5]

[M5] AI 대화창
     - 안내: "현재 송금이 안전한지 AI가 확인해드릴 수도 있어요.
             상황 설명, 대화 캡처, 통화 녹음 뭐든 올려주시면 확인해드릴게요."
     - 입력창(텍스트) / [📷 첨부] / [건너뛰기]
     - 첨부·입력 시: 로딩("AI가 확인 중이에요") → OCR/STT → RAG 분석
     - 완료 → [M6]

[M6] 최종 판정 + 요약 메시지 (3단계 취합 결과)
     - 🟢 정상: "안전하게 확인됐어요" + 간단 근거 → [송금하기] → [M7]
     - 🟡 주의: "이런 점이 걱정돼요" + 공식번호 직접확인 안내
                → [그래도 송금][취소] (송금 선택 시 [M7])
     - 🔴 위험: "보이스피싱이 의심돼요" + 사유 요약
                → [가까운 영업점 보기][112 신고][1332 상담]
                → 이체 보류 (영업외: 지연이체 예약 + 가족알림 안내)
                → 동시에 영업점 리포트 자동 생성(7장)

[M7] 송금 완료
     - 완료 메시지 + 판정 근거 한 줄 로그
```

### 6-2. 필수 구현 화면
`M1 송금액` · `M2 기관선택(+백그라운드 스코어링)` · `M3a 확인팝업` · `M4 질문카드` · `M5 대화창` · `M6 결과 3종(🟢🟡🔴)` · `M7 완료`. 그 외는 상태로 처리.

---

## 7. 영업점 연계 리포트 (고위험 판정 시 자동 생성)

🔴 위험 판정 시, 고객이 영업점 방문하면 창구 직원이 **상황을 즉시 파악**하도록 요약 리포트를 자동 생성한다. (팀 정의: "연계용 요약 리포트 자동 생성")

### 7-1. 리포트 항목

```
■ 고객 정보        : 성명, 연락처, (마스킹된) 출금계좌
■ 시도 거래        : 수취 금융기관·계좌, 예금주, 금액, 시도 일시
■ 최종 위험 판정   : 등급(위험) + 계좌위험/맥락위험 각각
■ 계좌 신호 근거   : 걸린 1단계 신호 목록 (예: 위험계좌 정보, 평소 대비 고액, 자금이동)
■ 대화 진단 요약   : 고객 응답 요약 + 업로드 자료 유무(캡처/녹음)
■ RAG 매칭 결과    : 추정 사기 유형, 매칭 사례, 감지된 위험신호(안전계좌·원격앱 등)
■ 권고 조치        : 예) 대면 본인확인, 통화상대 재확인, 필요시 지급정지·신고 안내
■ 생성 일시 / 리포트 ID
```

### 7-2. 형태
- 프로토타입에서는 **화면(웹) 또는 PDF 형태로 렌더링**해 시연. 실제 창구 단말 연계는 Mock.
- 출력은 구조화 JSON → 리포트 템플릿 렌더 방식. LLM이 '대화 진단 요약'만 자연어로 생성, 나머지는 데이터 매핑.

### 7-3. 예시(검찰사칭 케이스1 기준)
```
[멈칫 사기의심 거래 리포트]  #RPT-20260811-001
고객      : 남용환 / 010-****-**** / 351-****-0001
시도거래  : 미래에셋증권 010-6660-98261 (예금주: 남용환) / 20,000,000원 / 2026-08-11 01:10
판정      : 위험 (계좌위험 高 90 / 맥락위험 高 110)
계좌근거  : 위험계좌 정보(+40), 평소 대비 고액(+25), 자금이동 적금해지(+25)
대화요약  : "검찰이 안전계좌로 옮기라 했다"고 응답, 안내문자 캡처 1건 업로드
RAG매칭  : 기관사칭(검찰사칭)형, 사례 S02 유사도 0.87 / 위험신호: 안전계좌·자산보호
권고      : 대면 본인확인 및 통화상대 진위 확인, 필요시 지급정지·112 안내
```

---

## 8. Mock / 실제 구분 (완성도 시연 포인트)

| 기능 | 상태 | 비고 |
|---|---|---|
| 대화형 UI / 화면 흐름 | **실제** | React |
| Agent 오케스트레이션 | **실제** | LLM Tool Use (Anthropic/Bedrock) |
| OCR (이미지→텍스트) | **실제** | Tesseract(로컬)/Textract(AWS) |
| 시나리오 RAG | **실제** | FAISS(로컬)/Bedrock KB(AWS), 사례집 10건 |
| 텍스트 입력 분석 | **실제** | Claude |
| STT (음성파일) | 데모 수준 | 깨끗한 샘플로 시연 |
| 사기계좌 조회(더치트/ASAP) | **Mock** | 2-2 수취계좌 DB |
| 거래패턴/한도/기기 | **Mock** | 2-1 고객 데이터 |
| 영업점 리포트 | **실제** | 데이터 매핑 + LLM 요약 → 화면/PDF 렌더 (창구 단말 연계만 Mock) |
| 영업점 연계 큐 | **Mock** | 가짜 큐에 리포트 적재 |

> 실서비스 전환 시 Mock을 실데이터·실API로 대체 = 실무적용 로드맵.

---

## 9. 구현 순서 제안 (Claude Code용)

1. 프로젝트 스캐폴딩 (backend FastAPI / frontend React / mock_data)
2. Mock 데이터 4종 JSON 생성 (2장 스키마 + 5장 케이스값)
3. 1단계 8개 툴 + scoring.py (4장 로직)
4. 3단계 취합 함수 + 최종 판정
5. LLM Agent 오케스트레이터 (Tool Use, 프로바이더 추상화)
6. scenario_rag (사례집 정제 후 연결 — 초기엔 Mock 응답으로 스텁)
7. React 모바일 UI (6장 화면 M1~M7, 1단계 백그라운드 실행 포함) — **응답 소스 스위치(demo/local/remote) 구조로 구현**
8. 영업점 리포트 생성·렌더 (7장)
9. demo 모드 고정응답 5케이스 내장 (발표·배포용)
10. 5케이스 end-to-end 검증 (local·demo 양쪽)
